export enum routes {
  LOGIN = "/login",
  REGISTRATION = "/registration",
  HOME = "/",
  ACCOUNT = "/account",
  NEW_GROUP = "/new-group",
  FRIENDS = "/friends",
  MESSAGES = "/messages",
  TOOLS = "/tools",
  EXIT = "/exit",
}
