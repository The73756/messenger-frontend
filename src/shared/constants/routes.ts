export enum routes {
  LOGIN = "/login",
  HOME = "/home",
  ACCOUNT = "/account",
  NEW_GROUP = "/new-group",
  FRIENDS = "/friends",
  MESSAGES = "/messages",
  TOOLS = "/tools",
  EXIT = "/exit",
}
